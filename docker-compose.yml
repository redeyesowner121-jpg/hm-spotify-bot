version: "3.8"

services:
  bot:
    build: .
    container_name: hm-spotify-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    environment:
      - BROWSER_HEADLESS=true
      - DATABASE_PATH=data/bot.db

volumes:
  bot-data:
    driver: local
